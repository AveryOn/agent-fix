diff --git a/src/payment.service.ts b/src/payment.service.ts
index 4d21a71..dcab613 100644
--- a/src/payment.service.ts
+++ b/src/payment.service.ts
@@ -1,9 +1,19 @@
 export interface Payment {
   id: string
   providerEventId: string
   amount: number
   currency: string
 }

 export class PaymentService {
   private readonly payments: Payment[] = []
+
+  findByProviderEventId(
+    providerEventId: string
+  ): Payment | undefined {
+    return this.payments.find(
+      (payment) =>
+        payment.providerEventId === providerEventId
+    )
+  }

   create(input: Omit<Payment, 'id'>): Payment {
     const payment = {
       id: `payment-${this.payments.length + 1}`,
       ...input
diff --git a/src/webhook.handler.ts b/src/webhook.handler.ts
index c04593b..975e11b 100644
--- a/src/webhook.handler.ts
+++ b/src/webhook.handler.ts
@@ -18,6 +18,13 @@ export class PaymentWebhookHandler {
     if (webhook.type !== 'payment.succeeded') {
       throw new Error(`Unsupported webhook type: ${webhook.type}`)
     }

+    const existingPayment =
+      this.paymentService.findByProviderEventId(webhook.id)
+
+    if (existingPayment !== undefined) {
+      return existingPayment
+    }
+
     return this.paymentService.create({
       providerEventId: webhook.id,
       amount: webhook.data.amount,
       currency: webhook.data.currency
diff --git a/tests/payment-webhook.test.ts b/tests/payment-webhook.test.ts
index b2e49c4..c9843b1 100644
--- a/tests/payment-webhook.test.ts
+++ b/tests/payment-webhook.test.ts
@@ -34,6 +34,19 @@ describe('PaymentWebhookHandler', () => {
       'provider-event-002'
     )
   })
+
+  it('creates one payment for duplicate webhook delivery', () => {
+    const webhook = createWebhook()
+
+    webhookHandler.handle(webhook)
+    webhookHandler.handle(webhook)
+
+    expect(
+      paymentService.getPayments(),
+      'AGENT_FIX_REPRODUCTION: expected one payment'
+    ).toHaveLength(1)
+  })
 })
